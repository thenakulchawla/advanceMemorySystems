While root.isEmpty() || root.size()<bucketSize
  If yes then
    Check if rectangle completely falls into the root node:
    add the rectangle to the root node.
    Break;
  Else:
    Add rectangle to overflow list of root node.
    If no check in which quadrant the rectangle falls.
    Make Root = pointer of quadrant in which point falls.

